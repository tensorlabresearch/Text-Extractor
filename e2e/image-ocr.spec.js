// E2E: Drop a clean image and extract expected text.
import { test, expect } from '@playwright/test';

test('image OCR produces expected text', async () => {
  test.skip(true, 'Image OCR E2E pending — Phase 5');
});
