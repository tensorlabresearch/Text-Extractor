// E2E: Click toolbar icon and open workspace.
import { test, expect } from '@playwright/test';

test('toolbar click opens workspace tab', async ({ browser }) => {
  // TODO: implement with persistent Chromium context + unpacked extension
  // See Tab Recorder's e2e helpers for the pattern.
  test.skip(true, 'Extension E2E setup pending — Phase 1');
});
